apiVersion: v1beta3
kind: ReplicationController
metadata:
  name: grafana-v1
  namespace: default
  labels:
    k8s-app: Grafana
    version: v1
    kubernetes.io/cluster-service: "true"
spec:
  replicas: 1
  selector:
    k8s-app: Grafana
    version: v1
  template:
    metadata:
      labels:
        k8s-app: Grafana
        version: v1
        kubernetes.io/cluster-service: "true"
    spec:
      containers:
        - image: quay.io/samsung_ag/grafana:krak-62
          name: grafana
          env:
            - name: INFLUXDB_EXTERNAL_URL
              value: http://$INFLUXDB_EXTERNAL_HOST:8086/db/
            - name: INFLUXDB_HOST
              value: influxdb
            - name: INFLUXDB_PORT
              value: "8086"
